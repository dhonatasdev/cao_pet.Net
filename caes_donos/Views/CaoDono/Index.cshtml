@model IEnumerable<caes_donos.Models.CaoDono>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdCao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdRaca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdCao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdRaca)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDono }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDono }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDono })
        </td>
    </tr>
}

</table>
